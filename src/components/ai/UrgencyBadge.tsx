import type { UrgencyLevel } from '../../types/ai'
export function UrgencyBadge({ urgency }: { urgency: UrgencyLevel }) { const cls = urgency === 'Critical' ? 'bg-red-100 text-red-700' : urgency === 'High' ? 'bg-amber-100 text-amber-700' : urgency === 'Medium' ? 'bg-sky-100 text-sky-700' : 'bg-emerald-100 text-emerald-700'; return <span className={`rounded-full px-3 py-1 text-xs ${cls}`}>{urgency}</span> }
