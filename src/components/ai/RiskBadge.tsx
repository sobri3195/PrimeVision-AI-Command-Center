export function RiskBadge({ score }: { score: number }) { const cls = score >= 85 ? 'bg-red-100 text-red-700' : score >= 70 ? 'bg-amber-100 text-amber-700' : score >= 40 ? 'bg-sky-100 text-sky-700' : 'bg-emerald-100 text-emerald-700'; return <span className={`rounded-full px-2 py-1 text-xs font-semibold ${cls}`}>{score}</span> }
