import { Bell, Circle, UserRound } from 'lucide-react'
import type { DoctorRole } from '../../types/ai'

export function Topbar({ role, setRole, notificationsEnabled }: { search: string; setSearch: (v: string) => void; role: DoctorRole; setRole: (v: DoctorRole) => void; notificationsEnabled: boolean }) {
  const roles: DoctorRole[] = ['Doctor', 'Admin', 'Reviewer']
  const now = new Date().toLocaleDateString('en-US', { weekday: 'short', day: '2-digit', month: 'short', year: 'numeric' })
  return <header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 px-6 py-3 backdrop-blur"><div className="flex items-center justify-between"><div><h1 className="text-sm font-semibold text-slate-900">PrimeVision AI Command Center</h1><p className="text-xs text-slate-500">AI-powered clinical operations dashboard for eye care centers</p></div><div className="flex items-center gap-2 text-xs"><span className="inline-flex items-center gap-1 rounded-full border border-emerald-200 bg-emerald-50 px-2 py-1 text-emerald-700"><Circle className="h-2 w-2 fill-current" />System normal</span><span className="rounded-full border border-slate-200 px-2 py-1 text-slate-600">{now}</span><select value={role} onChange={(e) => setRole(e.target.value as DoctorRole)} className="rounded-full border border-slate-200 px-2 py-1 text-slate-700">{roles.map((r) => <option key={r}>{r}</option>)}</select><button className={`rounded-full border p-1.5 ${notificationsEnabled ? 'border-teal-200 text-teal-700' : 'border-slate-200 text-slate-500'}`}><Bell className="h-4 w-4" /></button><span className="inline-flex items-center gap-1 rounded-full border border-slate-200 bg-slate-50 px-2 py-1"><UserRound className="h-3 w-3" />Dr. Hana R.</span></div></div></header>
}
