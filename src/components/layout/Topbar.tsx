import { Bell, Circle, Search, UserRound } from 'lucide-react'
import type { DoctorRole } from '../../types/ai'

export function Topbar({ search, setSearch, role, setRole, notificationsEnabled }: { search: string; setSearch: (v: string) => void; role: DoctorRole; setRole: (v: DoctorRole) => void; notificationsEnabled: boolean }) {
  const roles: DoctorRole[] = ['Doctor', 'Admin', 'Reviewer']
  return <header className='sticky top-0 z-20 border-b border-slate-200 bg-white/85 px-6 py-3 backdrop-blur'><div className='mb-2 text-xs text-slate-500'>Command Center / Patients</div><div className='flex items-center justify-between gap-3'><div className='flex h-10 w-full max-w-xl items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3'><Search className='h-4 w-4 text-slate-500' /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder='Search across patients, MRN, branch...' className='w-full bg-transparent text-sm outline-none' /></div><div className='flex items-center gap-2 text-xs'><span className='inline-flex items-center gap-1 rounded-full border border-emerald-200 bg-emerald-50 px-2 py-1 text-emerald-700'><Circle className='h-2 w-2 fill-current' />System healthy</span><select value={role} onChange={(e) => setRole(e.target.value as DoctorRole)} className='rounded-lg border border-slate-200 px-2 py-1.5'>{roles.map((r) => <option key={r}>{r}</option>)}</select><button className='relative rounded-lg border p-2'><Bell className='h-4 w-4' /><span className='absolute -right-1 -top-1 h-4 w-4 rounded-full bg-red-500 text-[10px] text-white'>3</span></button><span className='inline-flex items-center gap-1 rounded-lg border border-slate-200 px-2 py-1.5'><UserRound className='h-3 w-3' />Dr. Hana</span></div></div></header>
}
