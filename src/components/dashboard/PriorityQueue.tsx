import type { Patient } from '../../types/patient'
import { RiskBadge } from '../ai/RiskBadge'

export function PriorityQueue({ data, onSelect, selectedId }: { data: Patient[]; onSelect: (id: string) => void; selectedId: string | null }) {
  return <section className="card p-4"><h3 className="mb-3 text-sm font-semibold">AI Priority Queue</h3><div className="space-y-2">{data.map((p, i) => <button key={p.id} onClick={() => onSelect(p.id)} className={`w-full rounded-xl border p-3 text-left ${selectedId === p.id ? 'border-teal-300 bg-teal-50/40' : 'border-slate-200 bg-white'}`}><div className="flex items-start justify-between"><div><p className="text-sm font-semibold text-slate-900">#{i + 1} {p.name}</p><p className="text-xs text-slate-500">MRN {p.medicalRecordNumber} · {p.recommendedSpecialist}</p></div><RiskBadge score={p.aiRiskScore} /></div><div className="mt-2 flex flex-wrap gap-1">{['OCT anomaly', 'Missed follow-up', 'Post-op red flag', 'Diabetes retina screening due'].slice(0, 2).map((c) => <span key={c} className="rounded-full border border-slate-200 bg-slate-50 px-2 py-0.5 text-[11px]">{c}</span>)}</div><div className="mt-2 flex items-center justify-between text-xs"><span className="text-slate-500">SLA countdown: 01h 45m</span><div className="flex gap-2"><span className="text-teal-700">Open Case</span><span className="text-slate-600">Assign Doctor</span><span className="text-slate-600">Mark Reviewed</span></div></div></button>)}</div></section>
}
